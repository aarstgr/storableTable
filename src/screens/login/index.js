import El from "@/utils/EL";
import { Navbar } from "@/Components";

export const LoginScreen =()=>{
    const main =El({
        element:'section',
        children:"Login Page Screen"
    })
    return main
}
